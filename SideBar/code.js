/*
var pagMenu = document.querySelectorAll('.pag-menu')

function selectLink(){
    pagMenu.forEach((item)=>
        item.classList.remove('selecionado')
    )
    this.classList.add('selecionado')
}

pagMenu.forEach((item)=>
    item.addEventListener('click', selectLink)
)

var btnExp = document.querySelectorAll('#expandir')
var menuSide = document.querySelectorAll('#side-bar')

btnExp.addEventListener('click', function(){
    menuSide.classList.toggle('expandir')
})*/
